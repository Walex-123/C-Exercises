#include <stdio.h>
#include <math.h>

   int main() {

  // int x = 3;
  // int y = 4;
   //float z = x / y;
   //int e = y % x;
  // x++;
   //y--;
   //x = x + 2;
  // x += 2;
  // printf("%f",z);
   //printf("%d",e);
   int a = 4;
   int b =sqrt(4);
   int c= pow(a,4);
   double d = sin(45);
   float e = ceil(9.203); //round up
   int f = floor(9.203); //round down
   int g = fabs(-2000); //absolute value
   printf("%d",g); //theres also log cos and tan.


    return 0;
   }